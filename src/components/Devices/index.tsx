import React, { useState } from 'react';
import { PinDialog } from './PinDialog';
import { BagButton } from './Button';

const BagPairer = () => {
    const [pinDialogVisible, setPinDialogVisible] = useState(false);
}